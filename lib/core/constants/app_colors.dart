import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFF010DFF); // Original blue color
  static const Color primaryDark = Color(0xFF000899);

  // Status Colors
  static const Color statusPreparing = Color(0xFFEEB128);
  static const Color statusArrived = Color(0xFF219653);
  static const Color statusRejected = Color(0xFFEB5757);
  static const Color statusCompleted = Color(0xFF27AE60);

  // Background Colors
  static const Color background = Color(0xFFFFFFFF);
  static const Color backgroundLight = Color(0xFFF8F9FA);
  static const Color backgroundDark = Color(0xFFF0F2F5);

  // Text Colors
  static const Color textPrimary = Color(0xFF050505);
  static const Color textSecondary = Color(0xFF65676B);
  static const Color textHint = Color(0xFFB0B3B8);

  // Border Colors
  static const Color border = Color(0xFFE4E6EB);
  static const Color borderLight = Color(0xFFEBEDF0);

  // Icon Colors
  static const Color icon = Color(0xFF65676B);
  static const Color iconLight = Color(0xFFB0B3B8);

  // Other Colors
  static const Color error = Color(0xFFFF3B30);
  static const Color success = Color(0xFF34C759);
  static const Color warning = Color(0xFFFFCC00);
  static const Color info = Color(0xFF5AC8FA);

  // Transparent
  static const Color transparent = Colors.transparent;
}
